

#include <iostream>
#include "CPPTools/LogSystem/LogSystem.h"


int main() {
//	LogSystem::Format("blob {}", 58);
	const std::forward_list<LogSystem::Values> list =  LogSystem::GetElements((uint8_t)5, 56);
	for(auto& i : list) {
		std::cout << LogSystem::GetStrValueType(i.type) << std::endl;
	}
}